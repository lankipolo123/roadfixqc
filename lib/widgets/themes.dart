import 'package:flutter/material.dart';

// lib/widgets/themes.dart

// Primary App Colors
const Color primary = Color.fromARGB(255, 247, 201, 13);
const Color secondary = Color.fromARGB(255, 3, 3, 3);
const Color altSecondary = Color.fromRGBO(0, 0, 0, 0.541);
const Color inputFill = Color.fromRGBO(255, 255, 255, 1);

// Status Colors (Used in reports, profile, etc.)
const Color statusDanger = Color(0xFFFF5252); // For rejected, error
const Color statusWarning = Color(0xFFFFAB40); // For pending
const Color statusSuccess = Color(0xFF4CAF50); // For resolved

// Additional Theme Colors (Reusable across app)
const Color redAccent = Color(0xFFFF5252); // Red accent color
const Color greenAccent = Color(0xFF4CAF50); // Green accent color
const Color orangeAccent = Color(0xFFFF9800); // Orange accent color
const Color purpleAccent = Color(0xFF673AB7); // Purple accent color
const Color tealAccent = Color(0xFF009688); // Teal accent color
const Color indigoAccent = Color(0xFF3F51B5); // Indigo accent color
const Color redDark = Color(0xFFF80101); // Dark red color

// Utility Colors
const Color transparent = Color.fromRGBO(0, 0, 0, 0);
