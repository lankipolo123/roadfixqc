import 'package:flutter/material.dart';
import 'package:flutter_svg/flutter_svg.dart';

class AuthBackground extends StatelessWidget {
  const AuthBackground({super.key});

  @override
  Widget build(BuildContext context) {
    return Stack(
      children: [
        // Fullscreen SVG background
        Positioned.fill(
          child: SvgPicture.asset('assets/widgets/2.svg', fit: BoxFit.cover),
        ),

        // Top warning stripe
        Positioned(
          top: 0,
          left: 0,
          right: 0,
          child: SvgPicture.asset(
            'assets/widgets/1.svg',
            fit: BoxFit.cover,
            height: 14,
          ),
        ),

        // Bottom warning stripe
        Positioned(
          bottom: 0,
          left: 0,
          right: 0,
          child: SvgPicture.asset(
            'assets/widgets/1.svg',
            fit: BoxFit.cover,
            height: 14,
          ),
        ),
      ],
    );
  }
}
